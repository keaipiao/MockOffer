# 本地开发：直连本机依赖、http（cookie 不加 Secure）
spring.datasource.url=jdbc:postgresql://localhost:5432/${POSTGRES_DB:mockoffer}
spring.data.redis.host=localhost
app.cookie.secure=false
app.frontend-base-url=http://localhost:3000
