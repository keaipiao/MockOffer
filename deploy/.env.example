# 复制为 deploy/.env 后按需修改。compose 已内置同样的默认值，
# 本文件仅作文档与本地覆盖入口；生产环境请改成强口令并通过部署平台注入。
POSTGRES_USER=mockoffer
POSTGRES_PASSWORD=mockoffer
POSTGRES_DB=mockoffer
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
