# 更新日志

> 版本变更总账。每次上线在**顶部**加一段（SOP 阶段 6 发布归档时写）。
> 详细变更不在此重复，指向对应功能目录即可。
>
> 格式：`## [vX.Y.Z] — YYYY-MM-DD · 功能名` + 几行要点 + 指向 `docs/功能/NN-功能名/`。

---

## [未发布]

（暂无）

---

## [v0.1.0] — 2026-06-16 · M1 账号体系

- 新增：邮箱验证码无密码登录、GitHub OAuth 登录、登录方式绑定 / 解绑 / 换绑邮箱。
- 新增：JWT 存 httpOnly cookie 的无状态鉴权 + jti 黑名单即时吊销 + refresh 轮换。
- 新增：前端登录页 / 工作台 / 账号设置，全站 **SSR + AuthProvider** 鉴权（首屏零闪烁）。
- 数据：新增 `users` / `user_identities` 两表（Flyway `V1`）。
- 详见 `docs/功能/01-账号体系/`
